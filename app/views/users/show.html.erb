<h1><% if admin? %><%= link_to "Users", :action => "index" %>
	&rarr; <% end %><%= @user.name %>&#8217;s Account</h1>

<%= flash_notice %>

<div class="details">
	<h2><%= h @user.name %> <%= image_for_admin_status(@user) %></h2>

	<p class="email"><strong><%= h @user.name %>&#8217;s registered email address:</strong> <%= mail_to h(@user.email) %></p>
<% if @w.shop? -%>
  <p><%= link_to(h(@user.name) + '&#8217;s orders', user_orders_path(@user)) %></p>
<% end -%>
<% unless @user.managed_website.nil? -%>
  <p>Website manager for <%= h @user.managed_website.name %></p>
<% end -%>
<% if admin? %>	
	<p class="modify">
		<%= link_to "edit", :action => "edit", :id => @user.id %> |
		<%= link_to "destroy", { :action => "destroy", :id => @user.id },
		 						 :confirm => 'Are you sure?',
								 :method => :delete %>
	</p>
<% end %>
</div>
