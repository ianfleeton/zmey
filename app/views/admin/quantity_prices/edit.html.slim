= page_header 'Edit Quantity/Price Rule'

= breadcrumbs({ 'Products' => admin_products_path, @quantity_price.product.name => edit_admin_product_path(@quantity_price.product) }, 'Edit Quantity/Price Rule')

= render 'form'
