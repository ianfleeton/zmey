= page_header 'Edit Additional Product'

= breadcrumbs({ 'Products' => admin_products_path, @additional_product.product.name => edit_admin_product_path(@additional_product.product) }, 'Edit Additional Product')

= render 'form'
