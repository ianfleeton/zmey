<h1><% if @product.pages.first %><%= link_to @product.pages.first.name, page_path(@product.pages.first) %> &rarr; <% end %><%= @product.name %></h1>
<%= flash_notice %>
<%= render :partial => 'product', :object => @product, :locals => {:full => true} %>
