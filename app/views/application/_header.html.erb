<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= @title ? @title : website.name %></title>
  <meta name="description" content="<%= @description %>">
  <%= robots_meta_tag(index: !@no_index, follow: !@no_follow) %>
  <%= website.use_default_css? ? stylesheet_link_tag('screen', media: 'screen', 'data-turbolinks-track' => true) : '' %>
  <%= stylesheet_link_tag('admin', media: 'screen') %>
  <%= website.css_url.blank? ? '' : stylesheet_link_tag(website.css_url, media: 'screen') %>
  <!--[if lt IE 9]>
  <script src="//html5shim.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->
  <%= javascript_include_tag('application', 'data-turbolinks-track' => true) %>
  <%= csrf_meta_tags %>
<% if @w.blog_id -%>
  <link rel="alternate" type="application/rss+xml" title="RSS 2.0" href="/forums/feed/<%= @w.blog_id_before_type_cast %>" />
<% end -%>
<%= render("shared/google_analytics_code") %>
</head>
<body id="<%= body_id %>" class="<%= body_class %><%= " admin-bar" if admin_or_manager? %>">
<div id="top"></div>
<div id="wrapper">
  <header role="banner">
    <a href="/"><span><%= website.name %></span></a>
  </header>
