<% if @quantity_price.errors.any? -%>
<div class="alert alert-error fade in">
  <a class="close" data-dismiss="alert" href="#">&times;</a>
  <h2><%= pluralize(@quantity_price.errors.count, "error") %> prohibited this quantity/price rule from being saved:</h2>

  <ul>
<% @quantity_price.errors.full_messages.each do |msg| -%>
    <li><%= msg %></li>
<% end -%>
  </ul>
</div>
<% end -%>

<%= form_for @quantity_price do |form| %>
  <%= form.hidden_field :product_id %>

	<%= form.label :quantity %>
	<%= form.text_field :quantity %>

	<%= form.label :price %>
	<%= form.text_field :price %> GBP

  <br><%= form.submit class: 'btn btn-primary' %>
<% end %>
