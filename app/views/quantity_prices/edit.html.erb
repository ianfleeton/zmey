<h1>Edit Quantity/Price Rule</h1>
<%= flash_notice %>
<div class="form">
	<%= error_messages_for :quantity_price -%>

	<fieldset>
		<legend>Edit quantity/price rule</legend>
    <%= form_for @quantity_price do |form| %>
    <%= render :partial => 'form', :object => form %>

    <%= submit_tag "Update Quantity/Price Rule" %>

  <% end %>
  </fieldset>
</div>
