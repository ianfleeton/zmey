<%= render :partial => 'products/product', :object => product, :locals => {
   :additional_products => additional_products,
   :full => false,
   :page => page,
   :placement => placement,
   :product_features => product_features
} %>
