<div class="page-header">
  <h1>New Shipping Rule</h1>
</div>

<ul class="breadcrumb">
  <li>
    <%= link_to 'Shipping Classes', shipping_classes_path %>
    <span class="divider">/</span>
  </li>
  <li>
    <%= link_to @shipping_table_row.shipping_class.name, edit_shipping_class_path(@shipping_table_row.shipping_class) %>
    <span class="divider">/</span>
  </li>
  <li class="active">
    New Shipping Rule
  </li>
</ul>

<% if @shipping_table_row.errors.any? -%>
<div class="alert alert-error fade in">
  <a class="close" data-dismiss="alert" href="#">&times;</a>
  <h2><%= pluralize(@shipping_table_row.errors.count, "error") %> prohibited this shipping table row from being saved:</h2>

  <ul>
<% @shipping_table_row.errors.full_messages.each do |msg| -%>
    <li><%= msg %></li>
<% end -%>
  </ul>
</div>
<% end -%>

<%= form_for @shipping_table_row do |form| %>
  <%= render :partial => 'form', :object => form %>

  <br><%= submit_tag "Save", class: 'btn btn-primary' %>

<% end %>
